plugins {
    id 'java'
    id 'application'
    id 'checkstyle'
    id 'org.openjfx.javafxplugin' version '0.1.0'
    id 'com.github.johnrengelman.shadow' version '7.1.2'
}

javafx {
    version = '21.0.1'
    modules = ['javafx.controls', 'javafx.fxml', 'javafx.graphics']
}

application {
    mainClass.set("Launcher")
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.10.0'
    testRuntimeOnly group: 'org.junit.jupiter', name: 'junit-jupiter-engine', version: '5.10.0'
}

checkstyle {
    toolVersion = '10.12.5'
    configFile = file('config/checkstyle/checkstyle.xml')
}

test {
    useJUnitPlatform()

    testLogging {
        events "passed", "skipped", "failed"

        showExceptions true
        exceptionFormat "full"
        showCauses true
        showStackTraces true
        showStandardStreams = false
    }
}

shadowJar {
    archiveBaseName = "javafx-tutorial-duke"
    archiveClassifier = null
    manifest {
        attributes 'Main-Class': 'Launcher'
    }
}

java {
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

run {
    standardInput = System.in
}

// So that "Run MainApp" from the IDE (Gradle JavaExec) also gets JavaFX
tasks.withType(JavaExec).configureEach { JavaExec task ->
    if (!task.jvmArgs.any { it?.contains('add-modules') }) {
        task.jvmArgs += ['--module-path', configurations.runtimeClasspath.asPath]
        task.jvmArgs += ['--add-modules', 'javafx.controls,javafx.fxml,javafx.graphics']
    }
}